.github/workflows/build-windows.ymlname: Build Windows EXE

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm install
      - run: npm run dist:win

      - uses: actions/upload-artifact@v4
        with:
          name: Bank-Slip-Converter-Windows
          path: dist/*.exe
