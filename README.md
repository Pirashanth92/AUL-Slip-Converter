# Bank Slip Converter Windows Build

Copy `index.html` into this folder. On GitHub, open Actions → Build Windows EXE → Run workflow. Download the `Bank-Slip-Converter-Windows` artifact. It contains both the portable `.exe` and the `Setup.exe` installer.
